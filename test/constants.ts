export const BURN_AMOUNT = Number(process.env.TEST_BURN_AMOUNT);

export const FULL_AMOUNT_BALANCE = Number(process.env.TEST_FULL_AMOUNT_BALANCE);

export const MINT_AMOUNT = Number(process.env.TEST_MINT_AMOUNT);

export const MAX_SAFE_AMOUNT = Number(process.env.TEST_MAX_SAFE_AMOUNT);

export const TRANSFER_AMOUNT = Number(process.env.TEST_TRANSFER_AMOUNT);

export const NULL_ADDRESS_OF_CONTRACT = process.env.TEST_NULL_ADDRESS_OF_CONTRACT;

export const ZERO_BALANCE = Number(process.env.TEST_ZERO_BALANCE);

export const CONTRACT_NAME = process.env.CONTRACT_NAME;
